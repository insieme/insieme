if(MSVC)
	file(GLOB_RECURSE integration_test_files ${CMAKE_CURRENT_SOURCE_DIR} *.c *.cpp *.h)
	add_custom_target(integration_test_files SOURCES ${integration_test_files})
	msvc_source_group("Files" "${integration_test_files}")
endif()
