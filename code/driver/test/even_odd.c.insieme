

//typedef int bool;
#define bool int

#define true 1
#define false 0

extern int printf(char *, ...);

bool even(unsigned x);
bool odd(unsigned x);

bool even(unsigned x) {
	return (x==0)?true:odd(x-1);
}

bool odd(unsigned x) {
	return (x==0)?false:even(x-1);
}


