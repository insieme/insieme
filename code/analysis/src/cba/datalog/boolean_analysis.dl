.using boolean_value from BooleanValue

// output values
.decl result( id:node, v : bool ) output

// link analysis with result
result( n, v ) :- boolean_value(n,DP_ROOT,v).

// .decl known_literalsout(id:node) output
// .using known_literals from BooleanValue
// known_literalsout(id) :- known_literals(id).

