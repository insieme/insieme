.using DefinitionPoint from Utils/VariableDefinitionPoint


// -- define the relation for the resulting definition point
.decl Result ( targetVar:variable, defPoint:variable ) output

// link the input with the output
Result(VAR,DEF) :- DefinitionPoint(DEF,VAR).
