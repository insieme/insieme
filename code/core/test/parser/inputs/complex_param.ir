
let int = int<4>;

// The following parameter list was making problems due to the , in the types.
let f = lambda (ref<array<int,1>> x, ref<vector<int,'a>> y)->unit {
	// nothing
};

let g = lambda (ref<array<int,1>> x, ref<vector<int,'a>> y)->unit {
    decl int a = 1;
};


unit main() {
		
}
