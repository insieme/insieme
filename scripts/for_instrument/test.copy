#include <stdio.h>
#include <stdbool.h>

#define bla printf("%d\n", i);

int main() {
	int i = 0;
	int j = 17;
	int x = 5;
	int w = 0;
	bool alb = false;

	for( int i = 0; i<x; ++i ) {
		bla
	}
	for (int i = 0, j; i<x && j > 8; ++i, j+=15) {
		bla
	}
	for(; i<x && j > 8; ++i, j+=15 )   {
		bla
	}
	for(int i = 0; i<x; ++i) 
	{
		bla
	}
	for   ( int i = 0      , j; i<x && j > 8; ++i, j+=15) 
	{
		bla
	}
	for(; (i<x) && j > 8; (++i), j+=15) 
	{
		bla
	}
	printf("ALPHA\n");
	for(int i = 0; (i<x); ++i) 
		bla;
	for(  int i = 0,j  ; ((i<x) && (j > 8))  ; ++i, j+=15) 
		bla;
	printf("BINARY SEARCH\n");
	for(; i<x && j > 8; ++i, j+=15) 
		bla;
	for(int i = 0; i<x; ++i) bla;
	printf("BINARY SEARCH 2\n");
	while(w<2) { w++; for(int i = 0, j; i<x && j > 8; ++i, j+=15) bla; }
	printf("BINARY SEARCH 2\n");
	for(; i<x && j > 8; ++i, j+=15) bla;

	printf("BETA\n");
	while(w<3) for(int i = 0; i<x; ++i) while(w<5) { w++; bla; }
	for(int i = 0; i<x; ++i) while(w<10) { { w++; bla; } };
	for(int i = 0; i<x; ++i) { while(w<12) { w++; bla; }; }
	for(int i = 0; i<x; ++i) while(w<16) for(int i = 0; i<x; ++i) { w++; bla; }
	return 0;
}
